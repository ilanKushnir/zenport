## What & why

## Checklist

- [ ] Tests added/updated (test-first for scanner/inference/planner/stats/auth/API changes)
- [ ] `npm test && npm run typecheck && npm run lint && npm run format:check` pass
- [ ] UI changes: screenshots at desktop and 390px; no horizontal overflow at 320px
- [ ] Never writes into a mounted library; no new outbound network calls
